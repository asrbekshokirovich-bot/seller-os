[project]
name = "selleros-scraper"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[project.urls]
plan = "Seller OS · Amalga oshirish rejasi v3"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
